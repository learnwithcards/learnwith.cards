// @flow
import {
  flex,
  horizontalCenter,
  fullWidth,
  horizontalPadding
} from './webpage-layout-rules'

export const wrapperStyles = [
  flex,
  horizontalCenter
].join('')

export const contentStyles = [
  fullWidth,
  horizontalPadding
].join('')

