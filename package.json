{
  "scripts": {
    "deploy:backend": "cd backend && source .env && graphcool deploy",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.js": ["standard --fix", "prettier-standard", "git add"]
  },
  "devDependencies": {
    "graphcool": "^0.11.6",
    "husky": "^0.14.3",
    "lint-staged": "^7.0.4",
    "prettier-standard": "^8.0.1",
    "standard": "^11.0.1"
  }
}
